% DIN 5008 Form B letter template for Pandoc
% Address window and layout positions match DIN 5008 Form B
\documentclass[a4paper, 11pt]{article}

\usepackage{fontspec}
\setmainfont{Arial}
\usepackage[ngerman]{babel}

\usepackage[
  top=20mm,
  left=25mm,
  right=20mm,
  bottom=25mm
]{geometry}

\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{fancyhdr}
\usepackage{parskip}
\usepackage{hyperref}
\usepackage{lastpage}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\footnotesize\textcolor{black}{\thepage\,/\,\pageref{LastPage}}}

% Suppress page number on first page (handled by letter layout)
\fancypagestyle{firstpage}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \fancyfoot[C]{\footnotesize\textcolor{black}{\thepage\,/\,\pageref{LastPage}}}
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\setcounter{secnumdepth}{0}

% Pandoc tight list support
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% --- Metadata from YAML ---
$if(sender-name)$
\newcommand{\sendername}{$sender-name$}
$endif$
$if(sender-street)$
\newcommand{\senderstreet}{$sender-street$}
$endif$
$if(sender-city)$
\newcommand{\sendercity}{$sender-city$}
$endif$
$if(sender-phone)$
\newcommand{\senderphone}{$sender-phone$}
$endif$
$if(sender-email)$
\newcommand{\senderemail}{$sender-email$}
$endif$
$if(sender-short)$
\newcommand{\sendershort}{$sender-short$}
$endif$
$if(recipient)$
\newcommand{\recipient}{$for(recipient)$$recipient$$sep$ \\ $endfor$}
$endif$
$if(date)$
\newcommand{\letterdate}{$date$}
$endif$
$if(city)$
\newcommand{\lettercity}{$city$}
$endif$
$if(subject)$
\newcommand{\lettersubject}{$subject$}
$endif$
$if(closing)$
\newcommand{\letterclosing}{$closing$}
$endif$

\usepackage{tikz}

\begin{document}
\thispagestyle{firstpage}

% === DIN 5008 Form B positioning ===
% All measurements from top-left corner of page (not text area)

\begin{tikzpicture}[remember picture, overlay]

  % --- Information block (top right, sender info) ---
  % Starts at 32mm from top, right-aligned to 20mm from right edge
  \node[anchor=north east, text width=60mm, align=right, font=\fontsize{9}{11}\selectfont]
    at ([xshift=-20mm, yshift=-32mm]current page.north east) {
    \textcolor{black}{%
      $if(sender-name)$\sendername\\$endif$%
      $if(sender-street)$\senderstreet, \sendercity\\$endif$%
      $if(sender-phone)$\senderphone\\$endif$%
      $if(sender-email)$\senderemail$endif$%
    }
  };

  % --- Return address line (small underlined) ---
  % Positioned to fit letterxpress address window
  $if(sender-short)$
  \node[anchor=west, font=\fontsize{7}{9}\selectfont]
    at ([xshift=20mm, yshift=-49mm]current page.north west) {
    \textcolor{black}{\underline{\sendershort}}
  };
  $endif$

  % --- Address zone (recipient address) ---
  % DIN 5008 Form B: starts 67mm from top (after 45mm + 5mm + 17mm zone)
  % 20mm from left, 85mm wide
  \node[anchor=north west, text width=85mm, align=left, font=\fontsize{11}{14}\selectfont]
    at ([xshift=20mm, yshift=-67mm]current page.north west) {
    $if(recipient)$\recipient$endif$
  };

\end{tikzpicture}

% --- Date line ---
% Body starts at 97.4mm from top = 97.4 - 20 (top margin) = 77.4mm from text top
\vspace*{77.4mm}

\begin{flushright}
\fontsize{10}{12}\selectfont
$if(city)$\lettercity, $endif$$if(date)$\letterdate$endif$
\end{flushright}

\vspace{8mm}

% --- Subject ---
$if(subject)$
{\fontsize{11}{14}\selectfont\textbf{\lettersubject}}

\vspace{6mm}
$endif$

% --- Body ---
\fontsize{10}{13}\selectfont
$body$

$if(sender-name)$
\par\vspace{6mm}
$if(closing)$
\letterclosing
$else$
Mit freundlichen Grüßen
$endif$
\par\vspace{15mm}
\sendername
$endif$

\end{document}
