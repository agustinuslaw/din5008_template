% DIN 5008 Form B letter template for Pandoc
% Address window and layout positions match DIN 5008 Form B
\documentclass[a4paper, 11pt]{article}

\usepackage{fontspec}
\setmainfont{Arial}
\usepackage[ngerman]{babel}

\usepackage[
  top=20mm,
  left=25mm,
  right=20mm,
  bottom=25mm
]{geometry}

\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{fancyhdr}
\usepackage{parskip}
\usepackage{hyperref}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\footnotesize\textcolor{black}{Seite \thepage}}

% Suppress page number on first page (handled by letter layout)
\fancypagestyle{firstpage}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \fancyfoot[C]{\footnotesize\textcolor{black}{Seite \thepage}}
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

% --- Metadata from YAML ---
$if(absender-name)$
\newcommand{\absendername}{$absender-name$}
$endif$
$if(absender-strasse)$
\newcommand{\absenderstrasse}{$absender-strasse$}
$endif$
$if(absender-ort)$
\newcommand{\absenderort}{$absender-ort$}
$endif$
$if(absender-telefon)$
\newcommand{\absendertelefon}{$absender-telefon$}
$endif$
$if(absender-kurz)$
\newcommand{\absenderkurz}{$absender-kurz$}
$endif$
$if(empfaenger)$
\newcommand{\empfaenger}{$for(empfaenger)$$empfaenger$$sep$ \\ $endfor$}
$endif$
$if(datum)$
\newcommand{\datum}{$datum$}
$endif$
$if(ort)$
\newcommand{\ort}{$ort$}
$endif$
$if(betreff)$
\newcommand{\betreff}{$betreff$}
$endif$

\usepackage{tikz}

\begin{document}
\thispagestyle{firstpage}

% === DIN 5008 Form B positioning ===
% All measurements from top-left corner of page (not text area)

\begin{tikzpicture}[remember picture, overlay]

  % --- Informationsblock (top right, sender info) ---
  % Starts at 32mm from top, right-aligned to 20mm from right edge
  \node[anchor=north east, text width=60mm, align=right, font=\fontsize{9}{11}\selectfont]
    at ([xshift=-20mm, yshift=-32mm]current page.north east) {
    \textcolor{black}{%
      $if(absender-name)$\absendername\\$endif$
      $if(absender-strasse)$\absenderstrasse, \absenderort\\$endif$
      $if(absender-telefon)$\absendertelefon$endif$
    }
  };

  % --- Rücksendeangabe (return address, small underlined) ---
  % Positioned to fit letterxpress address window
  $if(absender-kurz)$
  \node[anchor=west, font=\fontsize{7}{9}\selectfont]
    at ([xshift=20mm, yshift=-49mm]current page.north west) {
    \textcolor{black}{\underline{\absenderkurz}}
  };
  $endif$

  % --- Anschriftzone (recipient address) ---
  % DIN 5008 Form B: starts 67mm from top (after 45mm + 5mm + 17mm zone)
  % 20mm from left, 85mm wide
  \node[anchor=north west, text width=85mm, align=left, font=\fontsize{11}{14}\selectfont]
    at ([xshift=20mm, yshift=-67mm]current page.north west) {
    $if(empfaenger)$\empfaenger$endif$
  };

\end{tikzpicture}

% --- Date line ---
% Body starts at 97.4mm from top = 97.4 - 20 (top margin) = 77.4mm from text top
\vspace*{77.4mm}

\begin{flushright}
\fontsize{10}{12}\selectfont
$if(ort)$\ort, $endif$$if(datum)$\datum$endif$
\end{flushright}

\vspace{8mm}

% --- Subject ---
$if(betreff)$
{\fontsize{11}{14}\selectfont\textbf{\betreff}}

\vspace{6mm}
$endif$

% --- Body ---
\fontsize{10}{13}\selectfont
$body$

$if(absender-name)$
\par\vspace{6mm}
Mit freundlichen Grüßen
\par\vspace{15mm}
\absendername
$endif$

\end{document}
